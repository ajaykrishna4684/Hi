name: My First Work Flow
on: 
  push:
jobs:
  print-hi:
    runs-on: ubuntu-latest
    steps:
      - name: echo 
        run: echo "name=Ajay" >> $GITHUB_OUTPUT
      - name: echo 
        run: echo "age=35" ?? $GITHUB_env
      - name: latest
        run: ls