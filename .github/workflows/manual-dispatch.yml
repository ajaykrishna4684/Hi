name: Print GitHub Context

on:
  workflow_dispatch:

jobs:
  print-context:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        run: |
          echo '${{ toJson(github) }}' | jq .
