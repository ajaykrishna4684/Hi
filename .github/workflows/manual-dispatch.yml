name: My First Work Flow

on: 
  push:
    branches:
    - 'aj/*'
 
jobs:
  print-hi:
    runs-on: ubuntu-latest
    steps:
      - name: Print "github"
        run: |
          echo "${{github.ref}}" >>$GITHUB_OUTPUT
          echo "AJAY" >>$GITHUB_OUTPUT
      - name: Print GITHUB_OUTPUT
        run: echo ${{ GITHUB_OUTPUT }}
